{% extends 'SonataAdminBundle:CRUD:list.html.twig' %}

{% block list_filters %}
    
    {{ parent() }}
    
    <script type="text/javascript">
        jQuery(document).ready(function ($) {        
            $('label[for="filter_fecha_value_start"]').html('Desde');
            $('label[for="filter_fecha_value_end"]').html('Hasta');
        });        
    </script>
{% endblock %}
